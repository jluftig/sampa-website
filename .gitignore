# dependencies (any depth — root + mobile/)
node_modules/
.pnp
.pnp.js

# testing
coverage/

# production
dist/
dist-ssr/
.expo/
web-build/

# misc
.DS_Store
*.local
.venv/
venv/

# env files (real secrets stay local; .env.example is committed)
.env
.env*.local

# generated post drafts (sampa-post skill) — local only, never committed
/drafts

# cover A/B pick batches (local only; chosen file uploaded to Supabase)
docs/assets/stock/cover-picks-*/

# agent temp scripts
scripts/_tmp_*

# member data (this repo is PUBLIC — contact info must never be committed)
*member-import*
*Responses*.csv

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# session worktrees (other Claude sessions) — never commit
.claude/worktrees/

# OS / editor
.idea/
.vscode/
!.vscode/extensions.json
