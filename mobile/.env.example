# Copy this file to `.env.local` (gitignored) and fill in the values.
# Same values as the website's VITE_SUPABASE_* — the anon/publishable key is
# safe to ship because Row-Level Security is the real authorization boundary.
EXPO_PUBLIC_SUPABASE_URL=https://xbzzawjnphpnexwfjtif.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_...
