import React from 'react';
import ProtocolPlaceholder from './ProtocolPlaceholder';

export default function DtiPage() {
  return <ProtocolPlaceholder title="Direct-to-Inject (DTI) Buprenorphine" />;
}
