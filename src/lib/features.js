// Feature flags — temporary operational toggles.
//
// DONATIONS: keep api/create-donation-session.js in sync (same constant name
// at top of that file) so the API cannot create Checkout sessions while off.
//
// 2026-07-21: re-enabled after 501(c)(3) determination.

export const DONATIONS_ENABLED = true;
